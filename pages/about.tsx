import Hero from '../components/hero'
import { Container } from '@nextui-org/react'

const About = () => {
  return (
    <Container>
      <Hero title={'About'} subtitle={'development'} />
    </Container>
  )
}

export default About
